ChenYuHan's notes:
# git push my locally revised repository (origin) to GitHub (fork: yuhanc >> branch: yuhanc_project)
git checkout -b my-project
git add .
git commit -m "Initial project modifications"
git push -u origin my-project
